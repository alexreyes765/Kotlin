package com.itsrfid.cursokotlin

import  com.itsrfid.cursokotlin.Modelo.Usuarios

//comentarios
/*otra forma de comentarios
*
* el operador elvis ?: indica si una variable es nulla y hace lo que se indicque despues de eso*/

fun main() {
    /* println("Como te llamas perre?")
     val name = readln()
     println("Como te apellidas perre")
     val secondName = readln()
     println("Cual es tu correo perre")
     val email = readln()
     println("Tu contraseña perre")
     val pass = readln() */

    /*var mornigNotification = 22
    var eveningNotification = 100
    val usuario = Usuarios()
    usuario.ejercicio1(mornigNotification)
    usuario.ejercicio1(eveningNotification)
    */
    /*
    val child = 5
    val adult = 28
    val senior = 87

    val isMonday = true
    val usuario = Usuarios()
    println("El precio del boleto para esta persona con $child de edad es de \$${usuario.ejecicio2(child, isMonday)}")
    println("El precio del boleto para esta persona con $adult de edad es de \$${usuario.ejecicio2(adult, isMonday)}")
    println("El precio del boleto para esta persona con $senior de edad es de \$${usuario.ejecicio2(senior, isMonday)}")*/
}